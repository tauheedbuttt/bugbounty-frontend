
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';

export function SocialAccountsSettings() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Social Accounts</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-muted-foreground">Social account integrations will be available soon.</p>
      </CardContent>
    </Card>
  );
}
